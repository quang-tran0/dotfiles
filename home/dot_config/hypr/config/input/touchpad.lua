-- Touchpad settings (natural_scroll, etc.)

hl.config({
    input = {
        touchpad = {
            natural_scroll = true,
	    disable_while_typing = true,
	    scroll_factor = 0.5
	},
    },
})
