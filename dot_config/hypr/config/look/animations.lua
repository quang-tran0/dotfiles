-- Animations enable/disable.
-- Curves + per-property animations live in config/look/animations.lua too
-- (move them here in a later pass; they live alongside this file currently).

hl.config({
    animations = {
        enabled = true,
    },
})